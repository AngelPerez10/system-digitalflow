# Task 7 Report — Docs AGENTS + cleanup menor

## Done

- Added `### Plantilla de feature pages (programa arquitectura)` under **Arquitectura** in `AGENTS.md` (roadmap + Ola 1 Proyectos spec paths).
- Added two rows to **Archivos de referencia** table linking both spec files.
- Cleanup scan on touched Proyectos files (`form/`, `instalaciones/`, `ProyectosPage.tsx`): no `"solo diseño"` comments or unused imports found (prior tasks already cleaned modal imports).
- `pnpm exec eslint` on key touched files — exit 0.
- `pnpm exec tsc -b --noEmit` — exit 0.

## Files

| Action | Path |
|--------|------|
| Modify | `AGENTS.md` |

## Not committed

Per instructions — no git commit.
