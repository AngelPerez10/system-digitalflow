# Peer deps: el proyecto usa React 19; algunos paquetes aún declaran peers ^16–18.
# Equivalente pnpm de legacy-peer-deps (npm).
# Usar pnpm en la raíz del monorepo (pnpm-lock.yaml + pnpm-workspace.yaml).
strict-peer-dependencies=false
