# Equivalente pnpm de legacy-peer-deps (npm).
# expo-router arrastra peers de react/react-dom que no coinciden con el React
# fijado por Expo SDK 57; sin esto, `pnpm install` puede fallar por peers.
strict-peer-dependencies=false
